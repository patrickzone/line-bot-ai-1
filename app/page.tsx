export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: 32 }}>
      <h1>Keep Me Around — LINE Bot</h1>
      <p>บริการนี้มีไว้รับ webhook จาก LINE เท่านั้น ไม่มีหน้าเว็บสำหรับผู้ใช้</p>
    </main>
  );
}
